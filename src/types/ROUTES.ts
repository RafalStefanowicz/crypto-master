export enum ROUTES {
  CRYTPO_MASTER = "/crypto-master",
  STOCK = "/stock",
  RANK = "/ranking",
  INVESTMENTS_PARAMS = "/investments/:userName?",
  INVESTMENTS = "/investments",
  ACCOUNT = "/account"
}
