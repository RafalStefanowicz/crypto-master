// import { Page } from "./Page";
// import { connect } from "react-redux";

// import { IStore } from "../../redux/reducers";
// import { renderAlternative } from "../../routeConditions/renderAlternative";

// const _PageContainer = renderAlternative(() => null)(Page);

// const mapStateToProps = (state: IStore) => {
//   const renderAlternative = state.isLoggedIn === null;
//   return { renderAlternative };
// };

// export const PageContainer = connect(mapStateToProps)(_PageContainer);

export const changeIt = () => {};
